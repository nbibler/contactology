# encoding: utf-8

infinity_test do
  notifications :growl
  use :rubies => %w(1.9.2-p180 1.9.2-p290 1.8.7-p334), :gemset => 'contactology', :test_framework => :rspec

  before_run do
    clear :terminal
  end

  heuristics do
    add('lib/') do |file|
      run :all => :tests
    end
  end
end
